cmake_minimum_required(VERSION 3.10)
# root-level CMakeLists.txt, all project level initialization
# e.g. find_package(), enable_language(), include path, etc

# put it at the end
add_subdirectory(cu_tutor_addition)
add_subdirectory(cu_tutor_matmul)
